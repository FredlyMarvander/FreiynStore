<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Product</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f6f6f6;
            display: flex;
            justify-content: center;
            padding: 40px;
        }

        .card {
            display: flex;
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            padding: 30px;
            gap: 30px;
        }

        .product-image img {
            border-radius: 10px;
            width: 200px;
            height: auto;
        }

        .product-info {
            flex: 1;
        }

        .product-info h2 {
            margin: 0;
        }

        .back-link {
            font-size: 0.9em;
            color: gray;
            text-decoration: none;
        }

        .details {
            margin-top: 15px;
        }

        .details h4 {
            margin-bottom: 5px;
        }

        .price-section {
            margin-top: 20px;
        }

        .price {
            font-size: 1.5em;
            font-weight: bold;
        }

        .stock {
            margin-top: 10px;
            font-size: 0.95em;
            color: #444;
        }

        .buy-button {
            margin-top: 10px;
            padding: 10px 25px;
            background-color: #212121;
            color: white;
            font-size: 1em;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .buy-button:hover {
            background-color: #0056b3;
        }

        .sale {
            background-color: #f66;
            color: white;
            padding: 3px 7px;
            border-radius: 5px;
            font-size: 0.8em;
            margin-left: 10px;
        }
    </style>

</head>

<body>
    <div class="card">
        <div class="product-image">
            <img src="<%= data.image %>" alt="product">
        </div>
        <div class="product-info">
            <h2>
                <%= data.name %>
            </h2>

            <div class="details">
                <h4>Product Description</h4>
                <p></p>
            </div>



            <div class="price-section">
                <% if (data.type==="Handphone" ) { %>
                    <span class="price">
                        <%= total(data.price, 0.05) %>
                    </span>
                    <span class="sale">5% Off</span>
                    <% } else { %>
                        <span class="price">
                            <%= data.rupiah %>
                        </span>
                        <% }%>

            </div>

            <div class="stock">In stock: <%= data.stock %>
            </div>
            <br>
            <a style="text-decoration: none;" class="buy-button" href="/product/qrcode/<%= data.id %>">Buy</a>

        </div>
    </div>
    </div>
</body>

</html>